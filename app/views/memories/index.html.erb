<h1><%= @user.username %>'s Memories</h1>
<% @user.memories.each do |memory| %>
  <p><%= link_to memory.event.name, event_path(memory.event) %>: 
  <%= link_to memory.title, memory_path(memory) %></p>
<% end %>
