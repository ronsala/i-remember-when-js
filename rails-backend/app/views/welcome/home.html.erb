<center>
  <h1>I Remember When...</h1>
  <h3><em>Crowdsourcing first-hand history</em></h3>
  <%= image_tag("March on Washington from USMC-09611") %>
  <br>
  <p>The events of history shape our lives and our world. <em>I Remember When</em> is for people like you to share memories of events of historical importance within our time.</p>

  <% if !@new_memories.empty? %>
    <h5>Newly Added Memories:</h5>

    <table>
      <thead>
        <th>Event</th><th>Date</th><th>Title</th><th>Added</th>
    </thead>
    <% @new_memories.each do |new_memory| %>
      <tr>
        <td><%= link_to new_memory.event.name, event_path(new_memory.event) %></td>
        <td><%= format_date(new_memory.event.date) %></td>
        <td><%= link_to new_memory.title, memory_path(new_memory) %></td>
        <td><%= format_date(new_memory.created_at) %></td>
      </tr>
    <% end %>
    </table>
  <% end %>
</center>
